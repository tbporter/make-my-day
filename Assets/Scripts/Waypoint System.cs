using UnityEngine;
using System.Collections;


public class WaypointSystem : MonoBehaviour {

	// Use this for initialization
	void Start () {
		// Waypoint[] waypoints = gameObject.GetComponentsInChildren<Waypoint>();
		//GameObject platform = gameObject.GetComponentInChildren<Platform>();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
